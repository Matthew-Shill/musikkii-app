/**
 * @deprecated Import `useDashboardLessons` from `@/app/dashboard/hooks/useDashboardLessons` instead.
 * Kept so existing imports keep working.
 */
export { useDashboardLessons as useVisibleLessons, type DashboardLessonRow as VisibleLessonRow } from '@/app/dashboard/hooks/useDashboardLessons';
