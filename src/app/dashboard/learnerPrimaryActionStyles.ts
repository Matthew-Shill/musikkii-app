/**
 * Primary learner “Reschedule” actions — brighter than brand ink `#0331bd`.
 * Uses `theme.css` token (`--musikkii-blue-dark` → `#2563EB`).
 */
export const learnerReschedulePrimaryButtonStyle = { backgroundColor: 'var(--musikkii-blue-dark)' } as const;
